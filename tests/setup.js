// Global test setup
const sinon = require('sinon');

beforeEach(function() {
    // Global setup before each test
});

afterEach(function() {
    // Restore all stubs after each test
    sinon.restore();
});
